# 落雪音源下载器 — 完整交接文档

> 写于 2026-09-26。给接手的人/AI：**先读完这一份再动代码**，
> 里面很多坑是花了很多轮编译才换来的，重复踩一次要十几分钟。

---

## 一、这是什么

两个东西，别搞混：

| 项目 | 位置 | 说明 |
|---|---|---|
| **安卓 APK** | `apk_build/` | 图形界面，选音源 → 搜歌 → 试听/下载 |
| **命令行工具** | `lx_crawler/` | 终端里跑，交互式或 `-k 关键词` 直接下 |

两者**共用同一套音源 `.js`**（落雪/LX Music 的 user-API 格式），但 **JS 引擎完全不同**：

- APK → **Android WebView**（系统自带，现代 V8）
- CLI → **dukpy**（Python 内嵌 QuickJS，需 `pip install dukpy`）

音源文件在 `音源/` 目录，共 10 个，其中 6 个能正常工作。

---

## 二、音源是怎么工作的（核心概念）

音源 `.js` 只做一件事：**给我歌曲信息，我返回音频直链**。

```js
globalThis.lx = {
  EVENT_NAMES: {...},
  request(url, opts, cb),          // 宿主提供网络能力
  on(EVENT_NAMES.request, handler), // 音源注册处理器
  send(EVENT_NAMES.inited, {sources}), // 音源声明支持的平台
  env: 'mobile', version: '2.11.0',
  utils: { buffer, crypto }
}
```

**音源不提供搜索！** 搜索是我们自己实现的（见 `searchers.py`）。

### 支持的平台代号

| 代号 | 平台 | 搜索接口 |
|---|---|---|
| `wy` | 网易云 | `music.163.com/api/search/get/web` |
| `tx` | QQ音乐 | `u.y.qq.com/cgi-bin/musicu.fcg` |
| `kw` | 酷我 | `search.kuwo.cn/r.s`（**返回单引号字面量，不是 JSON**） |
| `kg` | 酷狗 | `mobilecdn.kugou.com/api/v3/search/song` |
| `mg` | 咪咕 | `app.c.nf.migu.cn/.../search_all.do` |

### 取直链时 musicInfo 需要的字段

```
wy: id
tx: songmid, strMediaMid
kw: id
kg: hash
mg: copyrightId
```

搜索函数会把平台专属字段放进返回值的 `extra`，解析时合并进 `musicInfo`。

---

## 三、代码结构（APK）

```
apk_build/
  main.py        556→~900 行  界面 + 编排（唯一入口）
  appenv.py      运行环境：日志/路径/权限/内置音源释放
  fonts.py       中文字体注册
  lxhost.py      注入 WebView 的 JS 宿主（纯字符串常量）
  lxbridge.py    WebView JS 引擎封装（线程规则在这里）
  searchers.py   5 个平台的搜索
  songinfo.py    品质/格式映射 + 直链探测 + 直链有效性验证
  qqresolve.py   QQ 专用解析器（内置代理列表）
  downloader.py  下载 + 三重校验
  netutil.py     HTTP（含 SSL 降级 + 记住证书有问题的主机）
  player.py      播放（Android MediaPlayer / 桌面 SoundLoader 兜底）
  test_ui.py     40 项冒烟测试（无真机、无显示器也能跑）
  assets/
    default_source.js           兜底单文件音源
    sources/src01..06_*.js      6 个内置音源（**文件名必须纯 ASCII**）
    fonts/NotoSansSC-subset.otf 自带简体中文字体（1.5MB）
  buildozer.spec
  .github/workflows/build.yml   云端编译 + 自动发布 Release
```

### 三条线程（最容易搞错的地方）

| 线程 | 职责 |
|---|---|
| **Android UI 线程（Java）** | WebView 的宿主线程。**创建 WebView 和调 `evaluateJavascript` 都必须在这上面** |
| **Kivy / Python 主线程** | 跑事件循环、改控件。**它不是 Android UI 线程！** |
| **后台工作线程** | 发起 JS 调用/搜索/下载，阻塞等结果 |

- 用 `@run_on_ui_thread`（`from android.runnable import`）投到 Java UI 线程
- **绝不能用 `Clock.schedule_once` 去调 WebView** —— 那是 Kivy 线程，会抛
  `AndroidRuntimeException: Calling WebView methods on a different thread...`
- 后台线程改界面一律走 `App.ui(fn)`

---

## 四、怎么编译 / 发版

**不需要在手机上装 SDK。** 用 GitHub Actions 云端编译。

| 项 | 值 |
|---|---|
| 仓库 | `github.com/vkxkzsp769-cloud/lx-apk` |
| 触发 | push 到 main，或手动 workflow_dispatch |
| 产物 | 编译完**自动**发布到 Releases（不需要人工上传） |

### 本地改完代码后的流程

```bash
SRC="/sdcard/我的文件/linux共享/落雪音源+源码/apk_build"
WORK=/tmp/push/lx-apk          # /sdcard 是 FUSE，git 必须在 /tmp 下操作
cd "$SRC" && rm -rf __pycache__ sources downloads builtin_sources
for f in main.py appenv.py fonts.py lxbridge.py lxhost.py searchers.py \
         downloader.py netutil.py songinfo.py player.py qqresolve.py \
         test_ui.py buildozer.spec; do cp -f "$f" "$WORK/"; done
cp -f assets/sources/*.js "$WORK/assets/sources/"
cp -f assets/fonts/*.otf  "$WORK/assets/fonts/"
cd "$WORK" && git add -A && git commit -m "..." && git push
```

**改版本号**：改 `buildozer.spec` 里的 `version = x.y.z`，
它同时决定 APK 版本名和 Release 的 tag。

### 推送注意

- SSH：`git@github.com:vkxkzsp769-cloud/lx-apk.git`（密钥 `~/.ssh/id_ed25519` 已配好）
- **开 VPN/代理时 SSH 22 端口会被拦**（报 `Connection closed by 198.18.0.9`）。
  两个办法：①关代理 ②临时走 HTTPS：
  `git push https://<用户名>:<TOKEN>@github.com/vkxkzsp769-cloud/lx-apk.git main`

### 本地测试（强烈建议改完先跑这个）

```bash
pip install "kivy[base]==2.3.0" pyflakes
cd apk_build
SDL_VIDEODRIVER=offscreen python test_ui.py     # 40 项，约 20 秒
```

**用 SDL offscreen 驱动可以在没有显示器的机器上真跑 Kivy 界面** ——
这是这套项目里最有价值的一个技巧，别丢。

---

## 五、⚠️ 踩过的坑（改代码前必读）

### A. python-for-android 相关的

| 坑 | 现象 | 必须怎么做 |
|---|---|---|
| p4a 没有 JS 引擎 recipe | dukpy/quickjs 都编不进 APK | **用 WebView 当 JS 引擎** |
| `android.extra_manifest_application_arguments` | 会套字面引号并把 `"` 转义成 `\"`，ManifestMerger 报 `Error parsing AndroidManifest.xml` | **别用它**。改用 workflow 里给 p4a 模板打补丁（见 build.yml 的「补丁 AndroidManifest」步骤） |
| p4a 模板位置 | 只在 edit/create 时渲染，缓存命中就不重渲染 | 要打补丁得同时补**所有** `AndroidManifest.tmpl.xml` + 已渲染的 manifest |
| `android.archs` | 双架构会让 openssl/python 各编两遍，超时 | **只留 `arm64-v8a`** |
| `p4a.branch = develop` | 该分支有 `libthorvg` recipe，NDK r25b 下 glob 返回空 → `IndexError` | **不要设** |
| `kivy==2.3.x` | 强依赖 thorvg，同上崩 | 只写 `kivy` 不锁版本 |
| sdkmanager 路径 | buildozer 1.5.0 硬编码 `tools/bin/sdkmanager`，新版装在 `cmdline-tools/latest/bin/` | workflow 里软链一份到老路径 |
| Java 版本 | 用 Java 11 跑 sdkmanager 报 `UnsupportedClassVersionError` | 全局 `JAVA_HOME` 指 Java 17 |
| `yes \| buildozer` | buildozer 跑完后 yes 仍写入 → SIGPIPE → 管道退出码 1 → 误判失败（其实 APK 已生成） | **不要用管道**，重定向到 build.log，用 `ls bin/*.apk` 判定成功 |
| 删 dist 强制重渲染 | 会触发 p4a 构建 venv 里 `pip` 的 `ImportError: BuildDependencyInstallError` | 不要删 dist |

### B. Kivy 相关的（这三条我各栽过一次）

1. **`canvas_before` 不是 Kivy 属性** —— 它是 `widget.canvas.before` 对象。
   当构造参数传会抛 `TypeError: Properties ['canvas_before'] passed to __init__`，
   而 `build()` 启动时立刻执行 → **App 秒退**。
   正确做法：建好控件后往 `widget.canvas.before` 加图元（见 `attach_bg`）。

2. **不要重新声明 Kivy 已有的属性**。我在 `CNSpinner` 里写了
   `font_name = StringProperty(None)`，把 `Label` 的默认值 `'Roboto'`
   覆盖成 `None` → `resolve_font_name()` 拿到 None 后崩
   （`AttributeError: 'NoneType' object has no attribute 'endswith'`）。
   **只在没注册中文字体时才触发**，很容易漏测。

3. **属性 observer 会在 `super().__init__()` 里就触发**。
   `FlatButton` 的 `bg_color` 通过 kwargs 传入，`on_bg_color` 在图元建好之前
   就被调用 → `AttributeError`。observer 里必须判空。

### C. Python 相关的

1. **`except Exception as e:` 的 `e` 在块结束时会被删除**，
   而 `Clock.schedule_once(lambda: ... e ...)` 是稍后执行的 → `NameError`。
   必须先 `msg = str(e)`。
2. **改代码时删 import 要小心**。我把 `downloader.py` 改成走 netutil 时
   删了 `import urllib.request` 和 `diag`，但新写的逻辑又用了它们 →
   **所有平台下载全部失败**，报 `NameError`。
   在线播放不经过那段代码，所以现象是「能听不能下」，极难联想。
   → 现在用 **pyflakes** 兜底（`test_no_undefined_names`）。

### D. Android / 网络相关的

1. **`.ttc` 字体的 face0 是日文**。`/system/fonts/NotoSansCJK-Regular.ttc` 里有 10 个 face，
   `face[0]=JP`、`face[2]=SC`，而 Kivy 的 SDL_ttf **只会打开 face 0** →
   中文用日文字形渲染（"中文字符显示错误"）。
   → **APK 自带裁剪过的 SC 字体**并优先使用。
2. **Android 文件名编码**：给内置音源起中文名，经 p4a 打包/解包后
   `os.listdir()` 拿到的是乱码 → **文件名必须纯 ASCII**，中文名放映射表。
3. **SSL 拦截**：有些网络的网关做 SSL 拦截（`self-signed certificate in chain`），
   Python 严格校验必然失败。`netutil` 会降级到不校验，并**记住出问题的主机**
   —— 否则每次请求都白等一次失败握手（10 秒以上），慢得没法用。
4. **明文 HTTP**：音源里很多是 `http://`，`targetSdk>=28` 默认禁止。
   manifest 里开了 `usesCleartextTraffic`，宿主 JS 也会先把 http 升级成 https。

### E. 第三方接口相关的

1. **网易云搜索**：单次上限 **100**（`limit>100` 直接返回空）；连续请求会返回
   `{"code":406,"msg":"操作频繁"}`（**HTTP 还是 200**，不识别就会误判成"没搜到"）；
   `offset` 超过总数后**会重复返回**前面的结果，必须按 id 去重。
2. **QQ 音乐**：
   - 官方 vkey 接口通，但多数歌返回 `result=104003`（要 VIP）
   - 各音源自带的第三方代理**有的活有的死**：
     `175.27.166.236/kgqq/qq.php` 实测 **403（死）**
     `175.27.166.236/kgqq1/qq.php` 实测 **可用**
   - 所以有了 `qqresolve.py`：内置代理列表，逐个试并逐个校验
   - level 映射：`standard`=128k / `exhigh`=320k / `lossless`=FLAC
3. **"能解析出直链" ≠ "直链能用"**。很多接口挂了会返回 403/网页，
   只看有没有拿到字符串会误判成功，最后在播放/下载时才炸。
   → **`songinfo.verify()` 必须用**（按音频魔数判断：ID3 / fFFB / fLaC / OggS / RIFF+WAVE / ftyp / MAC）。

---

## 六、我犯过的错（诚实记录，避免重犯）

| # | 错误 | 后果 |
|---|---|---|
| 1 | 做了「跨平台回退」：选 QQ 取不到就自动用网易云顶上 | **用户要 QQ，拿到网易云的音频** —— 这是设计错误，比直接失败更坏。已彻底移除，改成失败就如实报错 + 给「改用 XX」按钮由用户点 |
| 2 | 重做界面时漏传字体 `**self.F` | 搜索结果中文全是方块 |
| 3 | 状态标签命名 `self.log`，而 `set_status` 写 `self.status` | 状态栏**永远不更新**，界面看起来像卡死 |
| 4 | 为修 ANR 让 `_eval_sync` 在主线程直接返回 None | `load_source` 拿到 None → 「音源加载失败: 无响应」 |
| 5 | `evaluateJavascript` 用 `Clock.schedule_once` 调用 | 跨线程调 WebView → 「WebView 引擎不可用」 |
| 6 | 少 import `urllib`/`diag` | **所有平台下载全失败** |
| 7 | 测试写错导致「空跑」 | 字体检查因为没 import `Label` 抛 NameError 被 except 吞掉，永远通过。**修好后特意把 bug 放回去验证测试真能抓到** |
| 8 | 用「能不能解析出字符串」判断音源能力 | 得出「聚合音源 5 平台全通」的**错误结论**，实际只有 wy/kw |

**教训**：①断言要验证「测试真的能失败」 ②宁可如实报错也不要静默给出错误结果。

---

## 七、当前状态

### 能用的
- 启动、中文显示（自带 SC 字体）、音源下拉（6 个内置，可切换）
- 5 个平台**搜索**（wy/tx/kw/kg/mg）
- 播放（Android MediaPlayer，可拖进度）
- 下载（带平台 Referer + 直链有效性校验）
- 切音源（内置 6 个 + 从手机选 .js）

### 已知不稳定 / 待办
- **QQ 依赖第三方代理**（`kgqq1`），随时可能失效。失效后应更新 `qqresolve.QQ_PROXIES`
- **mg / tx 的直链**经常取不到（上游接口问题）
- 4 个音源在当前 dukpy 下加载失败（`cannot read property 'version' of undefined`）：
  野草、野花、独家v6、lx-music-source-v6。
  **但 APK 用的是 WebView（现代 V8），可能能跑** —— 可以用「更换」选它们试试
- `kw` 返回的 `SONGNAME` 带 `&nbsp;` 等实体，已做 `_clean()` 但不保证覆盖全部
- 下载目录写死 `Download/落雪音源`，**没有让用户选目录的功能**
- 音源×平台矩阵应**带 `verify()` 重跑一遍**（之前那版结论是错的）

### 下一步最该做的
1. 用 `verify()` 重跑音源×平台矩阵，找出每个平台真正能用的音源
2. 给 `qqresolve.QQ_PROXIES` 加更多备用代理，或做成可配置
3. 下载目录可选
4. 把 4 个加载失败的音源在 WebView 下测一遍

---

## 八、常用操作速查

```bash
# 跑测试（改完必跑）
cd apk_build && SDL_VIDEODRIVER=offscreen python test_ui.py

# 静态检查未定义名字
python -m pyflakes *.py

# 端到端（真实音源，桌面 dukpy 顶替 WebView）
python /tmp/e2e_new2.py

# 看真机日志（用户发回来的）
#   diag.log  — 关键步骤：字体、音源、解析、播放、下载尝试
#   crash.log — 异常堆栈
# 路径：Android/data/com.lxdl.lxdownloader/files/
```

**真机出问题时，第一件事是要 `diag.log`** —— 里面有
字体选择、音源加载、`QQ 解析成功 level=x size=x 耗时x`、
下载尝试的 status/Content-Type，基本能直接定位。

---

## 九、安全提醒

- 代码里出现过一个 GitHub Personal Access Token，用户应去
  <https://github.com/settings/tokens> 吊销
- 日常推送只用 SSH，不需要 token
