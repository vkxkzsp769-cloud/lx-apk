# 闪退 / 乱码 排查记录

## 一、乱码（中文显示成空方块）

**根因**：Kivy 默认字体是 Roboto，**不含任何中文字形**。

**修复**：
1. `register_cn_font()` 注册 Android 系统自带的中文字体
2. 字体候选顺序：`.otf`/`.ttf` 优先，`.ttc` 放最后
   —— `.ttc` 是字体集合，face0 通常是**日文**变体，中文会渲染成日文字形
3. `CNSpinner`：Kivy 原生 `Spinner` 的下拉列表项**不继承** `font_name`，
   导致收起时正常、展开后是方块。子类化后在 `_create_dropdown` 里统一套字体。

## 二、闪退（点搜索就退）

按确定性从高到低：

### 1. `_eval_sync` 主线程分支倒置（ANR → 进程被杀）
```python
if threading.current_thread() is threading.main_thread():
    self.wv.evaluateJavascript(js, cb)
    done.wait(timeout)     # ← UI 线程阻塞
```
在 p4a 上 **Kivy 事件循环就是 Android 主线程**。
`evaluateJavascript` 的 `ValueCallback` 是通过主线程 Looper 派发的 ——
阻塞主线程 = 回调永远送不到 = 卡死到 ANR = 系统杀进程。
→ 改为：主线程分支**不等待**，立即返回 `None`，由调用方走兜底路径。

### 2. `_on_engine_ready(False)` 在 UI 线程跑重活
WebView 失败时直接同步调 `load_source()` → `dukpy` 执行，
全部压在 UI 线程上。
→ 改为放到后台线程。

### 3. Clock 回调异常无人接管
Kivy **不捕获** `Clock.schedule_once` 回调里的异常，
未捕获异常会直接冒泡出事件循环并**终止 App**。
→ 新增 `safe_cb()`，包装全部 15 处 `schedule_once`。

### 4. WebView 跨域被 CORS 拦
宿主页原为 `https://localhost/`，从中发跨域 XHR 到 `music.163.com` 会被拦。
→ 改用 `http://localhost/` 作 base，并开启
`setAllowUniversalAccessFromFileURLs(True)`。

### 5. 明文 HTTP 被禁
音源里大量 API 是 `http://`，targetSdk ≥ 28 默认禁明文。
→ `buildozer.spec` 加 `android.uses_cleartext_traffic = True`。

## 三、顺带修掉

- `lx_engine.py`：超时检查位置修正，避免 Promise 不落定时 `while True` 无限空转
- 权限精简为 `INTERNET` + `ACCESS_NETWORK_STATE`
  （targetSdk 33 下 `WRITE/READ_EXTERNAL_STORAGE` 已是空操作，
   且本 App 只写 `getExternalFilesDir()`，本就免权限）
- 全量崩溃兜底：未捕获异常写入 `crash.log` 并显示在状态栏，不再静默闪退

## 四、UI 重构

- 深色主题，四分区：标题栏 / 控制卡片 / 结果列表 / 状态栏
- 固定标签宽度（"音源"/"平台"/"音质"）保证各行对齐
- 结果按钮 `text_size` 绑定宽度，修复 `halign` 不换行
- 布局高度预算：固定区 330dp，小屏 640dp 仍留 310dp 给列表
